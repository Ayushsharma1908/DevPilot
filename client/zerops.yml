zerops:
  - setup: frontend
    build:
      base: alpine/nodejs@22
      buildCommands:
        - npm install
        - npm run build
      deployFiles:
        - dist/~
    run:
      base: alpine/nodejs@22
